
var nome = 'Bianca';
var idade = 16;
console.log(nome, idade);
/*Variáveis são responsaveis por guardar dados na memória do computador.
Ela pode ser:
- var
- let
- const 

Elas servem justamente para evitar repetições*/
var preço = 5;
var totalComprado = 7;
total = preço * totalComprado;

var sobrenome = 'Bezerra',
    maça = 10;
total= sobrenome + maça

var semDefinir
console.log(semDefinir)

/*Os nomes das variáveis podem começar com $ ou _ ;
Não pode números no começo ;
Lembrar do Case sensitive -> tem que estar exatamente como você nomeou. 
Cameel case -> umaCoisa*/

//Hoisting -> erro, botar o console antes de dar a variável por exemplo.

